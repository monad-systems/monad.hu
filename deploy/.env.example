# Non-secret settings for docker compose. Secrets live as files in SECRETS_DIR.
CONTACT_IMAGE_TAG=sha-<commit sha from the Contact service workflow>
SCW_PROJECT_ID=<Scaleway project id that owns the Transactional Email domain>
MAIL_FROM=contact@monad.systems
MAIL_TO=hello@monad.hu
# SECRETS_DIR=/run/monad-systems/secrets
